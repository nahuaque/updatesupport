[project]
name = "updatesupport"
version = "0.1.0"
description = "Finite causal support adequacy and transport ambiguity in Python"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "scipy>=1.15.3",
]

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "hypothesis>=6.155.7",
    "pytest>=8.0",
    "ruff>=0.8",
]
